# Build a plane-detection scene

Modify `main.js` so that the app detects horizontal real-world planes (floors, tables) and places the cylinder from the starter template on the nearest horizontal surface as soon as one is found.

Only edit `main.js`. Do not touch `index.html` or the importmap.
