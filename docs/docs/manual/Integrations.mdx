---
sidebar_position: 16
title: Integrations
---

XR Blocks is designed to be modular and lightweight. Many features are enabled only when specific optional dependencies are installed in your project. This approach allows you to keep your bundle size small by including only the features you need.

## Optional Dependencies

The following table lists the optional dependencies supported by XR Blocks and the features they unlock.

| Feature                    | Dependency                                                               | Description                                                                                                                                    |
| :------------------------- | :----------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------- |
| **Physics**                | [`@dimforge/rapier3d`](https://www.npmjs.com/package/@dimforge/rapier3d) | Enables physics simulation for 3D objects, including rigid body dynamics, colliders, and gravity. Required for any physics-based interactions. |
| **Generative AI (Gemini)** | [`@google/genai`](https://www.npmjs.com/package/@google/genai)           | Enables integration with Google's Gemini models, including Multimodal Live API support for real-time audio/video interaction.                  |
| **Generative AI (OpenAI)** | [`openai`](https://www.npmjs.com/package/openai)                         | Enables integration with OpenAI's models for text generation and chat capabilities.                                                            |
| **UI System**              | [`lit`](https://www.npmjs.com/package/lit)                               | Used for rendering HTML-based UI overlays and the built-in Simulator UI components when running the simulator.                                 |
| **Text Rendering**         | [`troika-three-text`](https://www.npmjs.com/package/troika-three-text)   | Provides high-quality, signed-distance-field (SDF) 3D text rendering. Essential for readable and crisp text in 3D space.                       |
| **Gaussian Splatting**     | [`@sparkjsdev/spark`](https://www.npmjs.com/package/@sparkjsdev/spark)   | Enables viewing of Gaussian Splatting models, allowing for photorealistic scene rendering.                                                     |
| **UI Blocks**              | `@pmndrs/uikit`, `@preact/signals-core`                                  | Provides Yoga/flexbox-based rich spatial UI. Follow the complete UIBlocks import map because UIKit also has browser-side peers.                |
| **Vision**                 | `@mediapipe/tasks-vision`                                                | Enables MediaPipe object, human, face, hand-pose, and segmentation backends.                                                                   |
| **Audio classification**   | `@mediapipe/tasks-audio`                                                 | Enables on-device sound classification.                                                                                                        |
| **Accelerated raycasting** | `three-mesh-bvh`                                                         | Enables BVH-accelerated mesh raycasting where configured.                                                                                      |
| **Navigation**             | `three-pathfinding`                                                      | Supports simulator navigation meshes and path-constrained movement.                                                                            |

## Installing Integrations

Install only the dependencies needed by the features you use. Most integrations
are loaded lazily, but they are not universally auto-configured: physics needs
`options.physics.RAPIER`, flex UI needs its UIKit peer packages in browser import maps, and
camera/AI/vision capabilities must be enabled through `Options`.

For import-map applications, copy dependency entries from a working template
or sample. Several browser packages require multiple related specifiers; a
single top-level URL is not always sufficient.

For TypeScript projects that intentionally omit optional dependencies, use
`skipLibCheck` or install the relevant packages as development dependencies so
their imported declaration types resolve.
